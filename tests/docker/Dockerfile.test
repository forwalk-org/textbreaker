# Custom Dockerfile for textbreaker tests
FROM divio/multi-python:latest

USER root

# Install any base system tools if necessary (e.g. build-essential)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

USER tox
